{% extends 'front.html.twig' %}

{% block changer %}
<br>
<br>
<br>

<div class="container" style="margin-top:100px;">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title">Punition</h4>
                <p class="card-category">akouter  une punition </p>
            </div>
            <div class="card-body">

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('punichment_index') }}">Back to the list</a>
        </li>
    </ul>
            </div>
        </div>
    </div>
</div>
{% endblock %}
