{% extends 'front.html.twig' %}

{% block changer %}
<br>
<br>
<br>
<br>
<div class="container">
    <div class="col-md-12">
        <div class="card">

            <div class="card-header card-header-primary">
                <h4 class="card-title ">liste des enseignants</h4>
                {#                        <p class="card-category"> Here is a subtitle for this table</p>#}
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead class=" text-primary">
                        <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for teacher in teachers %}
            <tr>
                <td><a href="{{ path('teacher_show', { 'id': teacher.id }) }}">{{ teacher.id }}</a></td>
                <td>{{ teacher.nom }}</td>
                <td>{{ teacher.prenom }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('teacher_show', { 'id': teacher.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('teacher_edit', { 'id': teacher.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
                </div>
            </div>
        </div>
    </div>
    <ul>
        <li>
            <a href="{{ path('teacher_new') }}">Create a new teacher</a>
        </li>
    </ul>
{% endblock %}
