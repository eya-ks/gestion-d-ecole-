{% extends 'front.html.twig' %}

{% block changer %}
<br>
<br>
<br>

<div class="container" style="margin-top:100px;">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title">Eleve</h4>
                <p class="card-category">ajouter  un eleve</p>
            </div>
            <div class="card-body">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul class=>
        <li >
            <a href="{{ path('eleve_index') }}" >Back to the list</a>
        </li>
    </ul>
            </div>
        </div>
    </div>
</div>

{% endblock %}
