{% extends 'front.html.twig' %}

        {% block  changer  %}
    <br>
    <br>
    <br>
        <div class="container" style="margin-top:100px;">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <h4 class="card-title">Restaurant </h4>
                        <p class="card-category">Bienvenue à votre espace d'inscription aux services restaurant </p>
                    </div>
                    <div class="card-body">
        {{ form_start(form) }}
        <table id="tab">
            <tr>
                <td> Saisir votre username:</td> <td> {{ form_widget(form.username) }}</td>
            </tr>
            <tr>
                <td> Votre email:</td> <td> {{ form_widget(form.email) }}</td>
            </tr>
            <tr>
                <td> Votre mot de passe:</td> <td> {{ form_widget(form.password) }}</td>
            </tr>
            <tr>
                <td> Duree de votre abonnement:</td> <td> {{ form_widget(form.duree_abonnement) }}</td>

            <tr>
                <td>  <button id="associer" class="btn btn-secondary">s'incrire</button></td>

            </tr>
        </table>
    </center>


    {{ form_end(form) }}

    <div style="text-align: center; color: #8959A8" id="affecter" ><H1> Vous estes inscrit au service Restaurant! </H1></div>

                    </div>
                </div>
            </div>
        </div>
            </div>
    <script src="{{asset('jquery-min.js')}}"></script>
    <script>
        $("#affecter").hide();
        $(document).ready(
            $("#associer").click(
                function () {
                    $("#affecter").show();
                    $("#tab").hide();

                }
            )
        )

    </script>
{% endblock %}