<script>
    function myFunction() {
        var input, filter, table, tr, td, i, txtValue;
        input = document.getElementById("recherche");
        filter = input.value.toUpperCase();
        table = document.getElementById("recherchetable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[1];
            if (td) {
                txtValue = td.textContent || td.innerText;
                if (txtValue.toUpperCase().indexOf(filter) > -1) {
                    tr[i].style.display = "";
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
    }
</script>

{% block  changer  %}
    <br>
    <br>
    <br>
    <br>

    <div class="col-md-12">
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title ">liste des messages</h4>
                {#                        <p class="card-category"> Here is a subtitle for this table</p>#}
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <input type="text" placeholder="rechercher" id="recherche" onkeyup="myFunction()">
                    <table class="table" id="recherchetable">
                        <thead class=" text-primary">
                        <th>  id Contact</th>
                        <th>  nom contact</th>
                        <th>prenomContact</th>
                        <th>emailcontact</th>
                        <th >idUser</th>
                        <th >supprimer</th>

                        <tbody>
                        {% for f in contact %}


                            <tr>
                                <td> {{ f.id }} </td>
                                <td> {{ f.nom }} </td>
                                <td> {{ f.prenom }} </td>
                                <td> {{ f.email }} </td>
                                <td> {{ f.telephone }} </td>
                                <td><a href="{{ path('update_contact',{'id':f.id}) }}" >  <button class="btn btn-secondary">Modifier</button></a></td>
                                <td><a href="{{ path('delete_contact',{'id':f.id}) }}" >  <button class="btn btn-secondary"> Supprimer</button></a></td>
                            </tr>

                        {% endfor %}
                        <h1>nombre des messages = {{ nbr }}</h1>

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock changer %}
